/**
 * @module domain/tools
 */
/**
 * Enum which holds all valid tool ids.
 * @readonly
 * @enum {String}
 */
export const Tools = Object.freeze({
	MEASURING: 'measuring',
	DRAWING: 'drawing',
	SHARING: 'sharing',
	IMPORT: 'import',
	EXPORT: 'export'
});
