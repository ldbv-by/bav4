/**
 * Enum which holds all valid tool ids.
 * @enum
 */
export const Tools = Object.freeze({
	MEASURING: 'measuring',
	DRAWING: 'drawing',
	SHARING: 'sharing',
	IMPORT: 'import',
	EXPORT: 'export'
});
