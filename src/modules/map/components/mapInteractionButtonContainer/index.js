import { MapInteractionButtonContainer } from './MapInteractionButtonContainer';
if (!window.customElements.get(MapInteractionButtonContainer.tag)) {
	window.customElements.define(MapInteractionButtonContainer.tag, MapInteractionButtonContainer);
}
